# ********RoostGPT********

# Test generated by RoostGPT for test karate-test using AI Type Open AI and AI Model gpt-4-turbo
# 
# ROOST_METHOD_HASH=75c9169c0c
# ROOST_METHOD_SIG_HASH=7a25c565c6
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: WiFi Troubleshooting for Fiber Internet Customers',
#   background: 'Background:\n' +
#     '    Given the customer support representative has access to the service activation and configuration API\n' +
#     '    And the customer support representative has access to the service test management API',
#   rule: null,
#   scenario: {
#     title: 'Scenario: Diagnose and resolve weak WiFi signal',
#     steps: 'Given a customer with subscriber ID "SVTAL001" reports a weak WiFi signal\n' +
#       'When the support representative initiates a diagnostic test\n' +
#       'Then the service test API should return a test ID\n' +
#       'And the diagnostic result should indicate low WiFi signal strength\n' +
#       'When the support representative uses the configuration API to change the WiFi channel\n' +
#       'Then the configuration API should return a success response\n' +
#       'And the customer should report improved WiFi signal strength',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  processor: "./processors.js"
  environments:
    development:
      target: "http://dev.example.com"
    staging:
      target: "http://staging.example.com"
    production:
      target: "http://api.example.com"
  payload:
    path: "DiagnoseAndResolveWeakWiFiSignal.csv"
    fields:
      - "subscriberId"
      - "newWiFiChannel"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Diagnose and resolve weak WiFi signal"
    flow:
      - post:
          url: "/serviceTest/initiate"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            subscriberId: "{{ subscriberId }}"
          log: "Initiating diagnostic test for weak WiFi signal."
          expect:
            - statusCode: 201
            - responseBody:
                hasProperty: "testId"
      - think: 1
      - get:
          url: "/serviceTest/results/{{ testId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          log: "Checking diagnostic results for weak WiFi signal."
          expect:
            - statusCode: 200
            - responseBody:
                hasProperty: "result"
                result: "low WiFi signal strength"
      - think: 1
      - post:
          url: "/serviceConfiguration/changeWiFiChannel"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            subscriberId: "{{ subscriberId }}"
            newChannel: "{{ newWiFiChannel }}"
          log: "Changing WiFi channel to resolve weak signal."
          expect:
            - statusCode: 200
            - responseBody:
                hasProperty: "status"
                status: "success"
      - think: 1
      - log: "Customer reports improved WiFi signal strength after channel change."
