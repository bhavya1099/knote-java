# ********RoostGPT********

# Test generated by RoostGPT for test karate-test using AI Type Open AI and AI Model gpt-4-turbo
# 
# ROOST_METHOD_HASH=9adbc3cb1f
# ROOST_METHOD_SIG_HASH=b70f07ae36
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Check broadband service availability and selection',
#   background: 'Background: \n' +
#     '    Given a customer base where each customer has a unique subscriber ID',
#   rule: null,
#   scenario: {
#     title: 'Scenario: Selecting a broadband package based on availability',
#     steps: 'Given "John Doe" received the broadband availability report\n' +
#       'And he finds that "BL_1000MB" broadband speed is available at his address\n' +
#       'When John selects the "BL_1000MB" package\n' +
#       'Then the system should validate the selection\n' +
#       "And confirm the broadband package for John's address",
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: '{{ $processEnvironment.API_HOST }}'
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: SelectingBroadbandPackageBasedOnAvailability.csv
    fields:
      - subscriberId
      - catalogId
    skipHeader: true
    delimiter: ^|^
  defaults:
    headers:
      Authorization: '{{ $processEnvironment.AUTH_TOKEN }}'
  plugins:
    expect: {}
scenarios:
  - name: Selecting a broadband package based on availability
    flow:
      - post:
          url: '{{ $processEnvironment.API_HOST_1 }}/availabilityCheck'
          json:
            customer:
              subscriberId: '{{ subscriberId }}'
            address:
              id: '2449'
              complement:
                complements:
                  - type: ES
                    value: ARC201
          log: Submitting availability check for John Doe
          expect:
            - statusCode: 200
            - hasProperty: resource
            - hasProperty: resource.products
            - equals:
                jsonPath: $.resource.products[0].catalogId
                value: '{{ catalogId }}'
      - think: 1
      - log: John finds BL_1000MB broadband speed available at his address
      - post:
          url: >-
            {{ $processEnvironment.API_HOST_0
            }}/listener/configurationResultEvent
          json:
            notification:
              customer:
                subscriberId: '{{ subscriberId }}'
              date: '2021-11-25T18:31:06.857-03:00'
              configuration:
                action:
                  type: HGW_WIFI_CONFIGURATION
                id: 96070797-c019-45e1-ac42-48a4a76022bb
                startDate: '2021-11-25T17:50:39-02'
                endDate: '2021-11-25T17:50:41-02'
                state: FINISHED
                diagnostic:
                  code: OK
          log: >-
            John selects the BL_1000MB package and system is processing the
            configuration
          expect:
            - statusCode: 204
      - think: 1
      - log: >-
          System should validate the selection and confirm the broadband package
          for John's address
